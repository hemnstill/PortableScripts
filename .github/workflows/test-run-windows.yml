name: test-run windows

on: [pull_request]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2

      - name: test-run
        shell: cmd
        run: |
          .tools\busybox64.exe bash .tests\test-run.sh